name: Polymarket Factory Research
on:
  push:
    paths:
      - 'research/**'
      - '.github/workflows/research.yml'
  workflow_dispatch:

concurrency:
  group: cumulative-alpha-research
  cancel-in-progress: true

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Inventory repeated state-constraint market families
        run: python research/state_family_inventory.py
      - name: Inventory exact multi-horizon YouTube state checkpoints
        run: python research/youtube_horizon_inventory.py
      - name: Backtest chronological YouTube horizon transitions
        run: python research/youtube_transition_backtest.py
      - name: Upload research outputs
        uses: actions/upload-artifact@v4
        with:
          name: research-output
          path: |
            state_family_inventory.json
            youtube_horizon_inventory.json
            youtube_transition_backtest.json
